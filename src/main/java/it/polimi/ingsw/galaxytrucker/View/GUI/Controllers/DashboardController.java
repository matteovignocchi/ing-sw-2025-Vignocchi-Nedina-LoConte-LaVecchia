package it.polimi.ingsw.galaxytrucker.View.GUI.Controllers;

//import javafx.fxml.FXML;
//
//import javax.swing.text.html.ImageView;
//import java.lang.classfile.Label;
//import java.util.List;

public class DashboardController {
//
//    @FXML
//    private ImageView ship1, ship2, ship3, ship4;
//
//    @FXML private Label speedLabel1, pointsLabel1, cargoLabel1, damageLabel1;
//    @FXML private Label speedLabel2, pointsLabel2, cargoLabel2, damageLabel2;
//    @FXML private Label speedLabel3, pointsLabel3, cargoLabel3, damageLabel3;
//    @FXML private Label speedLabel4, pointsLabel4, cargoLabel4, damageLabel4;
//
//    private List<ImageView> ships;
//    private List<Label> speedLabels, pointLabels, cargoLabels, damageLabels;
//
//    @FXML
//    public void initialize() {
//        ships = List.of(ship1, ship2, ship3, ship4);
//
//        speedLabels = List.of(speedLabel1, speedLabel2, speedLabel3, speedLabel4);
//        pointLabels = List.of(pointsLabel1, pointsLabel2, pointsLabel3, pointsLabel4);
//        cargoLabels = List.of(cargoLabel1, cargoLabel2, cargoLabel3, cargoLabel4);
//        damageLabels = List.of(damageLabel1, damageLabel2, damageLabel3, damageLabel4);
//    }
//
//    public void updateShipParams(int shipIndex, int speed, int points, String cargo, int damage) {
//        if (shipIndex < 0 || shipIndex >= 4) return;
//        speedLabels.get(shipIndex).setText("Speed: " + speed);
//        pointLabels.get(shipIndex).setText("Points: " + points);
//        cargoLabels.get(shipIndex).setText("Cargo: " + cargo);
//        damageLabels.get(shipIndex).setText("Damage: " + damage);
//    }
//
//    public void updateShipPosition(int shipIndex, int boardPosition) {
//        if (shipIndex < 0 || shipIndex >= 4) return;
//        ImageView ship = ships.get(shipIndex);
//        ship.setVisible(true);
//        double x = computeX(boardPosition);
//        double y = computeY(boardPosition);
//        ship.setLayoutX(x);
//        ship.setLayoutY(y);
//    }
//
//    private double computeX(int pos) {
//        // Adatta in base al tracciato
//        return 100 + pos * 20;
//    }
//
//    private double computeY(int pos) {
//        return 200;
//    }
}

